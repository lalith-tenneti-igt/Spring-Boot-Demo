import com.example.model.GamePlay

rule "Check gamePlayNumber is larger than 5"
when
    GamePlay($gamePlayNumber : gamePlayNumber > 5)
then
    System.out.println("GamePlayNumber is larger than 5: " + $gamePlayNumber);
end
